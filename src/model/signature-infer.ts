import { MonoApi } from "../runtime/api";
import { MonoMethod } from "../model/method";
import { getParameterCount, verifyParameterCount } from "../utils/common-utilities";

// Re-export for backward compatibility
export { getParameterCount, verifyParameterCount };
